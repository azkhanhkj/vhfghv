name: VPS-Windows-2022-Roblox
on: workflow_dispatch

jobs:
  build:
    runs-on: windows-2022
    steps:
      - name: 1. Cài đặt Tailscale
        run: |
          Invoke-WebRequest -Uri "https://pkgs.tailscale.com/stable/tailscale-setup-latest.exe" -OutFile "tailscale-setup.exe"
          Start-Process -FilePath "tailscale-setup.exe" -ArgumentList "/quiet", "/norestart" -Wait
          
      - name: 2. Kết nối mạng Tailscale
        run: |
          & "C:\Program Files\Tailscale\tailscale.exe" up --authkey ${{ secrets.TAILSCALE_AUTHKEY }}
          
      - name: 3. Tạo User azkhanhkj_depzai
        run: |
          $User = "azkhanhkj_depzai"
          $Pass = "Namsoclo@2026" 
          net user $User $Pass /add
          net localgroup administrators $User /add
          net localgroup "Remote Desktop Users" $User /add

      - name: 4. CÀI SẴN ROBLOX VÀ HIỆN IP
        run: |
          # Tải bộ cài Roblox Player về Desktop của User mới
          $DesktopPath = "C:\Users\azkhanhkj_depzai\Desktop"
          if (!(Test-Path $DesktopPath)) { New-Item -ItemType Directory -Path $DesktopPath }
          
          Write-Host "Đang tải Roblox..."
          Invoke-WebRequest -Uri "https://www.roblox.com/download/client" -OutFile "$DesktopPath\RobloxPlayerLauncher.exe"
          
          Write-Host "------------------------------------------"
          $tsIP = (Get-NetIPAddress -InterfaceAlias 'Tailscale' -AddressFamily IPv4).IPAddress
          Write-Host "IP CỦA BẠN: $tsIP"
          Write-Host "User: azkhanhkj_depzai | Pass: Namsoclo@2026"
          Write-Host "------------------------------------------"

      - name: 5. Duy trì máy (6 tiếng)
        run: |
          sleep 21600
