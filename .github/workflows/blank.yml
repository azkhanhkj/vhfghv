name: VPS-Windows-2022-Azkhanh
on: workflow_dispatch

jobs:
  build:
    runs-on: windows-2022
    steps:
      - name: 1. Cài đặt Tailscale
        run: |
          Invoke-WebRequest -Uri "https://pkgs.tailscale.com/stable/tailscale-setup-latest.exe" -OutFile "tailscale-setup.exe"
          Start-Process -FilePath "tailscale-setup.exe" -ArgumentList "/quiet", "/norestart" -Wait
          
      - name: 2. Kết nối mạng Tailscale
        run: |
          # Nhớ thêm TAILSCALE_AUTHKEY vào GitHub Secrets của Repo
          & "C:\Program Files\Tailscale\tailscale.exe" up --authkey ${{ secrets.TAILSCALE_AUTHKEY }}
          
      - name: 3. Tạo User azkhanhkj_depzai
        run: |
          $User = "azkhanhkj_depzai"
          $Pass = "Namsoclo@2026" 
          
          # Tạo user và cấp quyền cao nhất
          net user $User $Pass /add
          net localgroup administrators $User /add
          net localgroup "Remote Desktop Users" $User /add
          
          Write-Host "XONG! User: $User | Pass: $Pass"

      - name: 4. Duy trì máy (6 tiếng)
        run: |
          Write-Host "VPS đang chạy. Kết nối qua IP trên app Tailscale!"
          sleep 21600
